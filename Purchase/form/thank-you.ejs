<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Thank You - Higher India</title>
    <style>
        /* Google Forms style */
        body {
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #f0f4f8;
            margin: 0;
        }

        .container {
            text-align: center;
            background: white;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
            max-width: 400px;
        }

        .checkmark {
            font-size: 50px;
            color: #34a853; /* Google green color */
        }

        h2 {
            font-size: 24px;
            color: #202124;
            margin: 10px 0;
        }

        p {
            font-size: 16px;
            color: #5f6368;
        }

        .company {
            font-size: 18px;
            font-weight: bold;
            color: #1a73e8; /* Google blue color */
            margin-bottom: 10px;
        }

        .home-link {
            display: inline-block;
            margin-top: 20px;
            text-decoration: none;
            color: white;
            background-color: #1a73e8;
            padding: 10px 20px;
            border-radius: 5px;
            font-weight: bold;
        }

        .home-link:hover {
            background-color: #1257c4;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="checkmark">✔</div>
        <div class="company">Higher India</div>
        <h2>Thank you for your submission!</h2>
        <p>Your quotation has been received successfully.</p>
        <a href="/" class="home-link">Go back to Home</a>
    </div>
</body>
</html>
